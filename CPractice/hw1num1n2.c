#include <stdio.h>
#include <stdlib.h>

int main(int argc, const char * argv[]) {

    // /*
    // Assignment #1: Question #1
    // Write a short program that asks the user for an integer.
    // Calculate the square and cube of the integer (x2, x3) and print those values out for the user.
    // */

    // int num; //Variable num to store user integer
    // printf("Enter a number to be squared & cubed: ");
    // scanf("%d", &num); //Stores user input

    // int squared = num * num;                          //Variable squared is assigned to user integer input
    // printf("Your number squared is: %d \n", squared); //Prints user input, squared

    // int cubed = num * num * num;                 //Variable cubed is assigned to user integer input
    // printf("Your numbercubed is: %d \n", cubed); //Prints user input, cubed

    // /*
    // Assignment #1: Question #4
    // Create a short program that asks the user for 2 integers.
    // Create a const called PI and set that to 3.14. 
    // Return the sum of the users 2 integers multiplied by the constant PI.
    // */

    // int num1, num2; //Variables num1 & num2
    // const double PI = 3.14; //Constant float variable given the name PI assigned to 3.14

    // printf("Enter your first number: \n");
    // scanf("%d", &num1); //Stores user input first number
    // printf("Enter your second number: \n");
    // scanf("%d", &num2); //Stores user input second number

    // printf("\n"); //prints a new line

    // int sum = num1 + num2; //Variable sum adds the user input
    // printf("The sum of the numbers you entered is: %d \n", sum); //Prints sum of input   
    // printf("The sum multiplied by constant PI is: %f \n", sum * PI); //Prints sum multiplied by constant PI

    //printf("Everything is commented. \n");
    
    return 0;

}